/**
 * Legacy BeforeDashboard — replaced by OwnerDashboardView (ADMIN.C).
 * Kept as a no-op so stale import maps do not crash until regenerate.
 */
export default function BeforeDashboard() {
  return null;
}
