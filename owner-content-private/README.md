# Owner content private binaries (local only)

Place raw client materials here — **do not commit**:

```text
owner-content-private/
  raw/<packVersion>/
  normalized/<packVersion>/
  ready/<packVersion>/
  archive/
```

Schemas, templates, and empty `docs/owner-content/pack.json` stay in git.
